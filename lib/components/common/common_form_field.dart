import 'package:flutter/material.dart';

//하얀박스안 필드
class CommonFormFirld extends StatelessWidget {
  const CommonFormFirld({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
